query GetPostsByFeed($uid: UID!, $page: Int, $perPage: Int) {
  postsByFeed(uid: $uid, page: $page, perPage: $perPage) {
    count,
    items {
      uid,
      title,
      previewUrl,
      createdAt
    }
  }
}
