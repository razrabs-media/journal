import styled from '@emotion/styled'

export const Wrapper = styled.div`
  position: relative;
  cursor: pointer;
`

export const LinkSurface = styled.a`
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
`
